#!/bin/bash

set -e

if [ -z "$1" ]; then
   echo "This script converts a TSV into a CSV";
   echo "Append the input file name and then the output file name";
fi

/Users/maggiecrowner/ds2002-course-mqq9sb/mywork/03-scriptin/usr/bin/tr '\t' '.' < $1 > $2

